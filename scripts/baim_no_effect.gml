/// double baim_no_effect(move id, user, target, skill, move score);
// used for determining the utility of a move by the ai.

var move=argument0;
var user=argument1;
var target=argument2;
var skill=argument3;
var move_score=argument4;

return move_score;
