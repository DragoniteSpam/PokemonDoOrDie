/// void battle_debug(string);

// This is the exact same thing as regular debug except the text
// isn't echoed to the game window and we pretend the Action
// button has already been pressed by the player.

debug(argument0);
battle_advance();
